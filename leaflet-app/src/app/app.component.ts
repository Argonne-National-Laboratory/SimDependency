// Angular Core
import { Component } from '@angular/core';

// Interfaces
// import { CriticalInfrastructureLayer } from './interfaces/interfaces';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {

    // @ViewChild(MapComponent) mapComponent: MapComponent;

    // onGetFile(ciLayer: CriticalInfrastructureLayer) {
    // //ciLayer is passed up from FileUploadComponent and received by this component
    // //ciLayer will be passed down from this component to the child MapComponent
    // console.log("calling mapComponent addToLayerList");
    // this.mapComponent.addToLayerList(ciLayer);
  // }
}
